# GitHub Personal Access Token
# Required permissions: repo, read:org
GITHUB_TOKEN=your_github_token_here

# Optional: GitHub Organization (can also be set in config.json or CLI)
GITHUB_ORG=your-organization

# Optional: Default repositories to analyze (comma-separated)
GITHUB_REPOS=repo1,repo2,repo3

# Optional: Output directory for reports
GITHUB_OUTPUT_DIR=reports